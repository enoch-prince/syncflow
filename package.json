{
  "private": true,
  "name": "syncflow-workspace",
  "version": "0.1.0",
  "description": "Monorepo for SyncFlow packages",
  "packageManager": "pnpm@8.9.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.6",
    "@types/express": "^4.17.17",
    "@types/cors": "^2.8.19",
    "@types/pg": "^8.20.0",
    "@types/uuid": "^9.0.7"
  }
}
